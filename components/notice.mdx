import { notice } from "@/core"
import { Button } from "@material-ui/core"

# Notice

<Button color="secondary" onClick={async ()=> {
    await notice({
      title: "Notice done",
      content: "This is a Notice test"
    })
}}>Success Notice</Button>

<span>&nbsp;&nbsp;</span>

<Button color="primary" onClick={async ()=> {
    await notice({
      title: "Error done",
      severity: "error",
      content: "This is a Error Notice test"
    })
}}>Error Notice</Button>

``` ts
import { notice } from "@/core"
...
    await notice({
      title: "Error done",
      severity: "error",
      content: "This is a Error Notice test"
    })
```

``` tsx
import { notice } from "@/core"
import { Button } from "@material-ui/core"
...
<Button color="secondary" onClick={async ()=> {
    await notice({
      title: "Notice done",
      content: "This is a Notice test"
    })
}}>Success Notice</Button>
```

## Props
``` ts
interface Props {
  title: string
  severity?: "success" | "info" | "warning" | "error" | null
  content?: string
}
```
